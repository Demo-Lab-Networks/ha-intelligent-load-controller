[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings =
    error::DeprecationWarning:custom_components.intelligent_load_controller
